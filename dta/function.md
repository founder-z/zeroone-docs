# Function

### if

次のような式が表示されます。

「if(条件,その場合,それ以外)」

「条件」の部分にboolean型の式を入力します。「条件」でyesが返される場合、「その場合」の部分で設定した値が返されます。noの場合、「それ以外」で設定した値が返されます。これらの２つの中にさらにifを設定することでさらに条件分岐を設定することも可能です。

### not

次のような式が表示されます。

「not(表現)」

表現の部分にboolean型の値が設定されると、notによって結果を反転させることができます。



